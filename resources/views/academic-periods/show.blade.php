@extends('tablar::page')
@section('title', 'Detalle del periodo académico')
@section('content')
<div class="page-header d-print-none"><div class="container-xl"><div class="row g-2 align-items-center"><div class="col"><h2 class="page-title">{{ $academicPeriod->name }}</h2></div><div class="col-auto ms-auto d-print-none"><div class="btn-list"><a href="{{ route('academic-periods.index') }}" class="btn btn-outline-secondary">Volver al listado</a><a href="{{ route('academic-process-windows.create', ['academic_period_id' => $academicPeriod->id]) }}" class="btn btn-outline-secondary">Agregar ventana</a><a href="{{ route('academic-periods.edit', $academicPeriod) }}" class="btn btn-primary">Editar</a></div></div></div></div></div>
<div class="page-body"><div class="container-xl"><div class="row g-3"><div class="col-12 col-lg-5"><div class="card"><div class="card-header"><h3 class="card-title">Información general</h3></div><div class="card-body"><dl class="row mb-0"><dt class="col-sm-4">Código</dt><dd class="col-sm-8"><span class="badge bg-blue-lt">{{ $academicPeriod->code }}</span></dd><dt class="col-sm-4">Nombre</dt><dd class="col-sm-8">{{ $academicPeriod->name }}</dd><dt class="col-sm-4">Inicio</dt><dd class="col-sm-8">{{ $academicPeriod->start_date?->format('d/m/Y') }}</dd><dt class="col-sm-4">Cierre</dt><dd class="col-sm-8">{{ $academicPeriod->end_date?->format('d/m/Y') }}</dd><dt class="col-sm-4">Estado</dt><dd class="col-sm-8">{{ $statusOptions[$academicPeriod->status] ?? ucfirst($academicPeriod->status) }}</dd><dt class="col-sm-4">Vigente</dt><dd class="col-sm-8">{{ $academicPeriod->is_active ? 'Sí' : 'No' }}</dd></dl></div></div></div><div class="col-12 col-lg-7"><div class="card"><div class="card-header"><h3 class="card-title">Ventanas configuradas</h3></div><div class="table-responsive"><table class="table table-vcenter card-table"><thead><tr><th>Proceso</th><th>Apertura</th><th>Cierre</th><th>Estado</th></tr></thead><tbody>@forelse($academicPeriod->processWindows as $window)<tr><td>{{ $processOptions[$window->process_key] ?? $window->process_key }}</td><td>{{ $window->start_at?->format('d/m/Y H:i') }}</td><td>{{ $window->end_at?->format('d/m/Y H:i') }}</td><td><span class="badge {{ $window->is_enabled ? 'bg-green-lt' : 'bg-red-lt' }}">{{ $window->is_enabled ? 'Habilitada' : 'Deshabilitada' }}</span></td></tr>@empty<tr><td colspan="4" class="text-muted">Este periodo aún no tiene ventanas de calendario.</td></tr>@endforelse</tbody></table></div></div></div></div></div></div>
@endsection
